# Trellune works locally with no environment variables.
#
# Remote Cloudflare Access configuration is optional and belongs in a private
# .dev.vars file or encrypted Worker secrets, never in this file.
# ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com
# ACCESS_AUD=your-access-application-audience
